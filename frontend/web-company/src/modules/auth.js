// auth reducer

import { createAction, handleActions } from 'redux-actions';

// action type

// action creator

// initial state
const initialState = {};

// reducer
const auth = handleActions({}, initialState);

export default auth;
